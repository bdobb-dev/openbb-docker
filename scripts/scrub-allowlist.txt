# Exact substrings the scrub gate ignores — known-benign literals only.
# The CGNAT range constant (how tier detection classifies tailnet clients):
100.64.0.0/10
# Synthetic tailnet-range IP used in tests (not a real host):
100.100.100.100
# --- .env.example non-empty-value gate: legitimate non-secret defaults ---
# EODHD's published demo websocket key (from their docs and SDK demo code —
# documented safe to commit; scoped to AAPL/MSFT/TSLA/BTC-USD/ETH-USD/EURUSD
# only). Appears in credentials.env.example and live-grid/.env.example:
EODHD_API_KEY=OeAFFmMliFG5orCUuwAKQ8l4WWFQ67YX
# Placeholder text, not a real key (openbb-eodhd/.env.example):
EODHD_API_KEY=your_api_key_here
# A boolean flag, not a credential (api-auth.env.example):
OPENBB_API_AUTH=true
# A suggested default username, not a credential (api-auth.env.example):
OPENBB_API_USERNAME=openbb
# A port number, not a credential (credentials.env.example, Ep. 10):
KDB_PORT=5000
# --- Ep. 11: Delta Lake/MinIO connection shape, none of them credentials ---
# A port number (minio.env.example, tick-lab/.env.example):
DELTA_S3_PORT=9000
# A bucket name, not a secret:
DELTA_S3_BUCKET=openbb
# A boolean flag (TLS on), not a credential:
DELTA_S3_SECURE=true
# A Delta library name, not a secret:
DELTA_LIBRARY=openbb

# --- Ep. 11 line: security-master.env.example policy/budget defaults, none secrets ---
# Placeholder text, not a real secret -- the operator must rotate it:
SECURITY_MASTER_PREFLIGHT_SECRET=change-me
# Policy enum values, not credentials:
SECURITY_MASTER_SQL=enabled
SECURITY_MASTER_ACQUISITION=review
# Spec-default budgets, not credentials:
SECURITY_MASTER_FIRST_PAGE=100
SECURITY_MASTER_MAX_ROWS=10000
SECURITY_MASTER_TIMEOUT_MS=15000
SECURITY_MASTER_PER_PRINCIPAL_QUERIES=8
SECURITY_MASTER_SCAN_SLOTS=4
SECURITY_MASTER_MEMORY_LIMIT=1GB
SECURITY_MASTER_THREADS=2
SECURITY_MASTER_WORKER_POLL_S=2
